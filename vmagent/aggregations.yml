- match: 'http_requests_total'
  without: [request_id]
- match: 'db_queries_total'
  interval: 1m
  outputs: [total]
  without: [trace_id]
- match: 'errors_total'
  interval: 1m
  outputs: [total]
  without: [session_id]
