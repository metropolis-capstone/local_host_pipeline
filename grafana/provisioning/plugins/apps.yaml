apiVersion: 1

#this is essentially the provisioining of the plugin. this is all I know at this stage. On startup grafana auto reads yaml files?
apps:
  - type: 'trickl-trickl-app'
    disabled: false
    jsonData:
      # injected at container startup by ECS; locally override via GF_PLUGINS env var or docker-compose environment block
      apiUrl: ${SMART_METRICS_API_URL}